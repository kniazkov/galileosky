/**
 * @file idle.hpp
 * @brief Объявляет платформенную функцию перехода в бесконечный цикл ожидания.
 *
 * Один и тот же код приложения вызывает эту функцию на всех платформах.
 * Конкретная реализация выбирается при линковке: desktop-версия периодически
 * засыпает, а версия для микроконтроллера выполняет инструкцию nop.
 */

#pragma once

namespace platform {

[[noreturn]] void idle_forever();

}  // namespace platform
